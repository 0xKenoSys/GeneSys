#include <stdio.h>

#define MAX(a, b)((a) > (b) ? (a) : (b))

int main() {
    int x = 10, y = 20;
    printf("x 和 y 中的较大值是：%d\n", MAX(x, y));
    return 0;
}


//
// Created by Madeleine on 2026/1/31.
//