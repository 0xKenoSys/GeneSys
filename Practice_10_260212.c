#include <stdio.h>
int main() {
    for (int i = 1; i <= 5; i++) {
        for (int j = 1; j <= 5-i; j++) {
            printf(" ");
        }

        for (int j = 1; j <= 2 * i - 1; j++) {
            if (j == 1 || j == 2 * i - 1 || i == 5 ) {
            printf("*");
                }else {
                    printf(" ");
                }
        }
        printf("\n");

    }
    return 0;
}

//挑战题目：空心金字塔（The Hollow Pyramid）
//2. 你的任务（Code Challenge）
//编写一个 C 程序，使用嵌套的 for 循环（建议两层），打印出如下形状：
//规则细节：
 //* 高度固定： 5 行。
 //* 外层循环： 控制行数（从 1 到 5）。
 //* 内层循环（关键）：
   //* 每一行都要打印一些空格和星号。
   //* 空格规律： 每一行前面的空格数是 高度 - 当前行数。
   //* 星号规律（空心逻辑）：
     //* 第一行：只打印一个星号。
     //* 最后一行：全部打印星号（9 个星号，即 2 * 5 - 1）。
     //* 中间行（2, 3, 4 行）：只在第一个和最后一个位置打印星号，中间全是空格。

//3. 柯林的提示（给初学者的拐杖）
//这个题的难点在于内层循环里的 if 判断。
//for (int i = 1; i <= 5; i++) { // 外层循环控制行
    // 1. 先打印前面的空格
    //for (int j = 1; j <= 5 - i; j++) {
        //printf(" ");
    //}

    // 2. 打印星号和中间的空格
    //for (int j = 1; j <= 2 * i - 1; j++) {
        //if (j == 1 || j == 2 * i - 1 || i == 5) {
            // 如果是当前行的第一个、最后一个，或者是最后一行
            //printf("*");
        //} else {
            // 否则打印中间的“空心”部分
            //printf(" ");
        //}
    //}
    //printf("\n"); // 换行
//}


// Created by 14423 on 2026/2/12.